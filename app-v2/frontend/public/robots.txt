# Scalyo — https://scalyo.app
# The public site is fully open to crawling.
# The application and transactional surfaces (/app, /login, /register,
# /join, /paywall, /payment-success, /reset-password) are excluded from the index
# through the X-Robots-Tag: noindex header declared in public/_headers.
# They are deliberately NOT blocked here: a Disallow would prevent
# Googlebot from reading the noindex, and so from de-indexing already indexed pages.

User-agent: *
Allow: /

Sitemap: https://scalyo.app/sitemap.xml
