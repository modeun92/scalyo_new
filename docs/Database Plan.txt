Company
	id
	Company Name
	Country
	Main Currency
	info links (SNS, web, youtube etc.)
	Photo Image link (for logo)
Person
	id
	company-id
	First-Name
	Last-Name
	Title (Dr./Mr./Ms./Mrs./Mx./None)
	Language with Region
	Position (Job Title)
	Phone Number
	Email Address
	Photo Image link (for profile)
	info links (SNS, web, youtube etc. / optional)
	IsValid // absent or present
Profit (Invoice)
	company-id
	from company-id
	amount
	currency	// should never change, if it gets changed then we need to know when, from what currency
	issued-at
	description (json)
Profit-Planner : Profit
	interval // only about future

Churn
	company-id
	from company-id
	issued-at
	description
task_param
	order
	type string
		case priotiry: { low, middle, important, urgent }
Terminal_Task
	id
	assignees : Workers
	startedAt
	finishedAt
	// too small to be an item in Task management
	// it will be inside the content only
	// it is very optional to use.
Task_Viewer_Tab
	id
	description : json
Task_Viewer
	id
	Worker (by id)
	Task_Viewer_Tabs (by id)
Comment
	id
	who
	content : json
Milestone
	id
	priority : task_param
	assignees : Workers
	Clients (by id)
	createdBy (by id) : worker
	expected_duration
	difficulty
	duedate
	starting
	createdAt
	deletedAt
	startedAt
	finishedAt
	description : json // with this I can calculate how long it really took to proceed
		when on hold?
		what happened mean while?
		can be .md with mermaid
		can be .html
	Tasks (by id) // tasks or sub tasks
	logs : json
	Comments (by id)
Task : Milestone
	id
	status : task_param
Repeatable_Task : Task
	interval // only future
	// before confirmation and being present, there will be empty things a lot.
Issue (Interaction)
	From (person-id) // who approached first?
	To (person-id) //
	With-s (person-id) // multiple optional
	Description json // issue, conclusion
	$what-it-is
		1. it could be
			1) a simple question
			2) a trouble
			3) a suggestion
			4) 
	$rules
		1. it could be registered firstly only with "from"
		2. if "from" is worker then To shall be visitor and vice-versa
		3. the partners can be in "with"
		4. "To" is the one who gets itself involved first by clicking the button "accept"
		5. as worker or visitor, if it wants to join the issue, then it will be in "with", by clicking "join"
		6. as worker or visitor, you can add the partner, depending on the visibility.
		7. one issue can contain sevel sub-issues
	$ideas
		1. like github project management
			1) milestone
			2) progress in %
			3) tree graph
	$to-think
		1. the difference to "Task"
			1) priority
			2) scheduling
		2. the common with "Task"
			1) due date - optional for Issue
			2) duration
	$templates
		1. 
Worker : Person
	Clients (by id) // for what companies does it do CSM?
	Working Status // quit, vacation, available
	Assigned Tasks (by id)
	Authorities
	Vacations (by id)
Viewer : Person
	Client (by id)
	- Updating the profit
	- viewing the infos (only about its company)
	- declining the contract (from the organization)
Administrator : Worker
	- Managing the plan
	- Updating the profit
	- Viewing the performance
		- of workers
		- of its own organization
Manager : Worker
	- Managing the plan (only when granted)
	- Updating the profit
	- managing the team
	- managing the clients
	- managing the tasks
Employee : Worker
	- CSM jobs
	- Updating the profit (only for assigned)
Partner : Person
Plan
	id
	paidAt
	type int // with this, I can calculate until when it is left
Organization : Company
	Plans (by id)
	Workers (by id)
	Partners (by id) // they could be our workers or collaborators(free-lancer) that don't use our web service, but certainly referred.
	Clients (by id)
	Churns (by id)
	Profits (by id)
Client : Company
	Organization (by id) // maybe it could be multiple
	Viewers (by id)
	assignees (by id) : Worker// who manage(s) this client?
	Interactions (by id)
	IsValid (it can stop the contract)
Emoji
	type // emoji value
	person // who did that?
Message
	id
	From	person-id
	To		person-id
	emojis (by id)
	written-at
	read-at
Chat Room
	Persons (by id)
	Messages (by id)
	ChatInfos json // joining (by whom of whom), deleting (by whom), pinned (by whom)
	pinned message (by id)
